---
sidebar_position: 1
---

import {VERSION} from '../../../env.ts';
import {ExampleDirectory} from '@site/src/components/ExampleDirectory';
import {ExcelIcon} from '@site/src/components/Icons';
import {ExcelFile} from '@site/src/components/ExcelFile';

# 🏃 Getting started

The <ExcelIcon /> configuration is in `.xlsx` format.

## 📥 Download

Download the default <ExcelIcon /> <ExcelFile version={VERSION}>v{VERSION} configuration</ExcelFile>

## 🏠 Local

1. Navigate to <ExampleDirectory version={VERSION} />
2. Open the `new-campaign` folder
3. Copy and edit the default <ExcelIcon /> `config.xlsx`
