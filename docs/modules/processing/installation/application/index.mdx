---
sidebar_position: 4
---


import {APP_NAME} from '@site/constants';
import {AppDirectory} from '@site/src/components/AppDirectory';
import {
  AppIcon,
  MacOsIcon,
  NvidiaIcon,
  PythonIcon,
  UbuntuIcon,
  WindowsIcon,
} from '@site/src/components/Icons';
import {SizeInfo} from '@site/src/components/SizeInfo';
import {VenvScriptWindows} from '@site/src/components/VenvScriptWindows';
import {Fragment} from 'react';
import ReactPlayer from 'react-player';
// noinspection ES6PreferShortImport
import {VERSION} from '../../../../env.ts';

# 💻 Application

This section will guide you through the installation process for the 🧮 Processing module.

## 📥 Download

<ol>
  <li>{'Download the installation script (right click, save as) '}</li>
  <ul>
    <li>
      <WindowsIcon />
      {' Windows: '}
      <VenvScriptWindows version={VERSION} />
    </li>
    <li>
      <UbuntuIcon />
      {' Ubuntu / '}
      <MacOsIcon />
      {' macOS: '}
      <VenvScriptWindows version={VERSION} />
    </li>
  </ul>
  <li>Create a new folder on your local disk</li>
  <li>Place the script downloaded at step 1. in that folder</li>
  <li>
    {'Extract the archive (use '}
    <i>Extract here</i>
    {')'}
  </li>
  <li>
    {'Open the folder '}
    <AppDirectory version={VERSION} />
  </li>
  <li>{'Proceed to the next steps'}</li>
</ol>

## 🔮 Virtual Environment

<Fragment>
  A virtual environment is established to manage <PythonIcon /> Python
  dependencies.
</Fragment>

It needs creation only once but can be activated multiple times.

:::tip
You will need to repeat this step each time you open a new terminal session.
:::

<Fragment>
  ### `venv` on <WindowsIcon /> Windows
</Fragment>

Double-click on `start.bat` and wait for `venv` creation and activation.

<details>
  <summary>
    <WindowsIcon /> Windows screencast
  </summary>
  <ReactPlayer
    controls
    loop
    url="/screencasts/venv_windows.mp4"
  />
</details>

<Fragment>
  ### `venv` on <UbuntuIcon /> Ubuntu / <MacOsIcon /> macOS
</Fragment>

<ol>
  <li>
    <Fragment>Open a new terminal session</Fragment>
  </li>
  <li>
    {'Type '}
    <code>cd</code>
    {' followed by a '}
    <code>&lt;space&gt;</code>
    {' and the path to the '}
    <AppDirectory version={VERSION} />
    {' folder'}
  </li>
</ol>

#### Create `venv`

Execute the following command to create a virtual environment:

```bash
python3.10 -m venv venv
```

#### Activate `venv`

Run this command to activate the virtual environment:

```bash
. venv/bin/activate
```

<details>
  <summary>
    <UbuntuIcon /> Ubuntu screencast
  </summary>
  <ReactPlayer
    controls
    loop
    url="/screencasts/venv_ubuntu.mp4"
  />
</details>

<Fragment>
  ## 🧩 Modules <SizeInfo size={'~14 GB'} />
</Fragment>

<Fragment>
  Modules constitute the components of <AppIcon /> {APP_NAME} and occupy
  approximately 14 GB of disk space.
</Fragment>

<Fragment>
  ### Modules on <WindowsIcon /> Windows
</Fragment>

Install all modules sequentially using the following command:

```powershell
pnpm i; pnpm install:processing:windows
```

:::tip
Use right-click to paste in Windows terminals.
:::

<Fragment>

:::tip
For accelerated processing with
a <NvidiaIcon /> [Nvidia](https://www.nvidia.com/) compatible GPU, include
additional
dependencies:

```bash
pnpm install:processing:cuda:windows
```

:::

</Fragment>

<Fragment>
  ### Modules on <UbuntuIcon /> Ubuntu / <MacOsIcon /> macOS
</Fragment>

Install all modules at once with this command:

```bash
pnpm i && pnpm install:processing
```

<Fragment>

:::tip
For accelerated processing with
a <NvidiaIcon /> [Nvidia](https://www.nvidia.com/) compatible GPU, include
additional
dependencies:

```bash
pnpm install:processing:cuda
```

:::

</Fragment>

## 🗑️ Uninstallation

<Fragment>
  To remove the application and its dependencies, delete the{' '}
  <AppDirectory version={VERSION} /> folder.
</Fragment>

## ⏭️ Next Step

Proceed to the 🧩 [Modules](../../modules/) documentation.
