import {ExcelIcon} from '@site/src/components/Icons';

# Run autoclusters

This process automatically assigns numerical clusters (groups) to related
intervals based on your <ExcelIcon /> configuration settings.

These clusters will be visible in Visualisation as standard labels.
