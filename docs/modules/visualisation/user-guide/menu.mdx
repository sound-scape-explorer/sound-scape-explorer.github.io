import {VisMenuIcon} from '@site/src/components/VisMenuIcon';

# 🧭 Menu

> <VisMenuIcon name="import" />
> <VisMenuIcon name="settings" />
> <VisMenuIcon name="help" />
> <VisMenuIcon name="selection" />
> <VisMenuIcon name="colors" />
> <VisMenuIcon name="labels" />
> <VisMenuIcon name="time" />
> <VisMenuIcon name="details" />
> <VisMenuIcon name="audio" />
> <VisMenuIcon name="trajectories" />
> <VisMenuIcon name="indicators" />
> <VisMenuIcon name="digested" />

## <VisMenuIcon name="import" /> Import

Load a local `.h5` storage file.

## <VisMenuIcon name="settings" /> Settings

Change the user interface settings.

## <VisMenuIcon name="help" /> Help

Find help links and keyboard shortcuts.

## <VisMenuIcon name="selection" /> Selection

Select your band, integration, extractor and reducer.

## <VisMenuIcon name="colors" /> Colors

Color intervals based on index, timestamp, label.

## <VisMenuIcon name="labels" /> Labels

Filter intervals based on their labels.

## <VisMenuIcon name="time" /> Time

Filter intervals based on their timestamps.

## <VisMenuIcon name="details" /> Details

Show details about the selected interval.

## <VisMenuIcon name="audio" /> Audio

Show audio for the selected interval.

## <VisMenuIcon name="trajectories" /> Trajectories

Display trajectories.

## <VisMenuIcon name="indicators" /> Indicators

Display acoustic indicators.

## <VisMenuIcon name="digested" /> Digested

Display digested metrics.
