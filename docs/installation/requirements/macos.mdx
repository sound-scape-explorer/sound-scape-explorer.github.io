---
sidebar_position: 3
title: macOS
---

import {APP_NAME} from '@site/constants';
import {
  MacOsIcon,
  HomebrewIcon,
  CIcon,
  PythonIcon,
  CurlIcon,
  FfmpegIcon,
  NodeIcon,
  PnpmIcon,
  XcodeIcon,
  TerminalIcon,
} from '@site/src/components/Icons';

# <MacOsIcon /> macOS

The following steps have been tested on macOS 13, and macOS 14

## <HomebrewIcon /> Homebrew

Using the package manager <HomebrewIcon /> [Homebrew](https://brew.sh/) is
recommended to install the application requirements

### 🛠️ Procedure

1. Navigate to [brew.sh](https://brew.sh/)
2. Follow the instructions to install <HomebrewIcon /> Homebrew
3. Open a <TerminalIcon /> terminal and proceed through next steps

## 📦 Dependencies

Run the below command to install the dependencies and update your path

- <CIcon /> [C compiler](https://gcc.gnu.org/)
- <CurlIcon /> [curl](https://curl.se/)
- <FfmpegIcon /> [ffmpeg](https://www.ffmpeg.org/)
- <PythonIcon /> [Python](https://www.python.org/)
- <NodeIcon /> [Node.js](https://nodejs.org/)
- <PnpmIcon /> [pnpm](https://pnpm.io/)

```bash
brew install gcc curl ffmpeg python@3.10 node@20 pnpm && \
echo 'export PATH="/usr/local/opt/node@20/bin:$PATH" >> ~/.profile'
```

### <XcodeIcon /> Xcode command line tools

:::info
This step is optional. Context is unclear.
:::

## 🗑️ Uninstall

To remove the requirements, open a <TerminalIcon /> terminal window and run the
command

```bash
brew uninstall gcc curl ffmpeg python@3.10 node@20 pnpm
```

## ⏭️ Continue

You can now close your <TerminalIcon /> terminal and [install
{APP_NAME}](../application)
