---
sidebar_position: 2
---

import {VERSION, SIZE} from '../../env.ts';
import {APP_NAME} from '@site/constants';
import {SourceLink} from '@site/src/components/SourceLink';
import {AppDirectory} from '@site/src/components/AppDirectory';
import {
  WindowsIcon,
  UbuntuIcon,
  MacOsIcon,
  PythonIcon,
} from '@site/src/components/Icons';
import {SizeInfo} from '@site/src/components/SizeInfo';

# 💻 Application

This section will guide you through the installation process for the {APP_NAME}
application.

## 📥 Download <SizeInfo size={SIZE} />

1. Download <SourceLink version={VERSION} size={SIZE} />
2. Extract the archive (use _Extract here_)
3. Open the folder <AppDirectory version={VERSION} />
4. Proceed to the next steps

## 🔮 Virtual Environment

A virtual environment is established to manage <PythonIcon /> Python
dependencies.

It needs creation only once but can be activated multiple times.

### `venv` on <WindowsIcon /> Windows

Double click on `start.bat` and wait for `venv` creation and activation.

### `venv` on <UbuntuIcon /> Ubuntu / <MacOsIcon /> macOS

1. Open a new terminal session
2. Type `cd` followed by a `<space>` and the path to the <AppDirectory
       version={VERSION} /> folder
   :::tip
   Instead of manual entry, drag and drop the folder icon into your terminal.
   :::

#### Create `venv`

Execute the following command to create a virtual environment:

```bash
python3.10 -m venv venv
```

#### Activate `venv`

Run this command to activate the virtual environment:

```bash
. venv/bin/activate
```

:::tip
You will need to repeat this step each time you open a new terminal session.
:::

## 🧩 Modules <SizeInfo size={'~14 GB'} />

Modules constitute the components of {APP_NAME} and occupy approximately 14 GB
of disk space.

### Modules on <WindowsIcon /> Windows

Install all modules sequentially using the following command:

```powershell
pnpm i; pnpm install:front; pnpm install:processing:windows
```

:::tip
For accelerated processing with a compatible GPU, include additional dependencies:

```bash
pnpm install:processing:cuda:windows
```

:::

### Modules on <UbuntuIcon /> Ubuntu / <MacOsIcon /> macOS

Install all modules at once with this command:

```bash
pnpm i && \
pnpm install:front && \
pnpm install:processing
```

:::tip
For accelerated processing with a compatible GPU, include additional dependencies:

```bash
pnpm install:processing:cuda
```

:::

## 🗑️ Uninstall

To remove the application and its dependencies, delete the <AppDirectory
version={VERSION} /> folder.

## ⏭️ Continue

Proceed to the 🧩 [Modules](../../modules/index.mdx) documentation.
