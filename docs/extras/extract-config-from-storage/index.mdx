---
title: 📦 Extract Config from Storage
---

import {ExtractCommandBlock} from '@site/src/components/ExtractCommandBlock';
import {ExtractCommandInline} from '@site/src/components/ExtractCommandInline';
import {ExcelIcon, PythonIcon, StorageIcon} from '@site/src/components/Icons';
import {Title} from '@site/src/components/Title';
import {Fragment} from 'react';
import {VERSION} from '../../env';

# 📦 Extract <ExcelIcon /> Config from <StorageIcon /> Storage

<Title
  pre={['✨ Extras']}
  title={'📦 Extract Config from Storage'}
  version={VERSION}
/>

<Fragment>

:::warning
Ensure you open the 🧮 Processing module with the
provided <code>sse-processing-{VERSION}</code> script to enable
the <PythonIcon /> Python virtual environment.
:::

</Fragment>

## 🔍 Context

<Fragment>
  This command allows the extraction of the <ExcelIcon /> configuration file
  from an existing <StorageIcon /> storage file.
</Fragment>

<Fragment>
  The resulting `storage.config-{VERSION}.xlsx` file represents the{' '}
  <ExcelIcon /> configuration used in creating the <StorageIcon /> storage file.
</Fragment>

## 🚀 Execution

<ol>
  <li>
    {'Type '}
    <ExtractCommandInline />
    {', then add '}
    <code>&lt;space&gt;</code>
  </li>
  <li>
    {'Drag and drop your '}
    <StorageIcon />
    {' storage file into the terminal (or type path)'}
  </li>
  <li>
    {'Press '}
    <code>Enter</code>
  </li>
</ol>

### 📝 Example

<ExtractCommandBlock />
