---
sidebar_position: 3
title: Visualisation panels
---

import {Button} from '@blueprintjs/core';
import {
  Array,
  Calendar,
  Cog,
  ColorFill,
  DocumentOpen,
  EyeOpen,
  HeatGrid,
  Help,
  Layout,
  LayoutBalloon,
  Music,
  SelectionBox,
  SeriesFiltered,
  Tags,
} from '@blueprintjs/icons';

<div style={{display: 'grid', gridTemplateColumns: 'auto 1fr'}}>
  {/* prettier-ignore */}
  <div>
    - <Button icon={<Help />} /> (<span style={{color: 'red'}}>1</span>) Help
    - <Button icon={<Cog />} /> (<span style={{color: 'red'}}>2</span>) Settings
    - <Button icon={<DocumentOpen />} /> (<span style={{color: 'red'}}>3</span>) Open
    - <Button icon={<EyeOpen />} /> (<span style={{color: 'red'}}>4</span>) View
    - <Button icon={<ColorFill />} /> (<span style={{color: 'red'}}>5</span>) Colors
    - <Button icon={<Calendar />} /> (<span style={{color: 'red'}}>6</span>) Calendar
    - <Button icon={<Tags />} /> (<span style={{color: 'red'}}>7</span>) Tags
    - <Button icon={<SelectionBox />} /> (<span style={{color: 'red'}}>8</span>) Selection
    - <Button icon={<SeriesFiltered />} /> (<span style={{color: 'red'}}>9</span>) Temporal
    - <Button icon={<HeatGrid />} /> (<span style={{color: 'red'}}>10</span>) Heatmaps
    - <Button icon={<Music />} /> (<span style={{color: 'red'}}>11</span>) Audio
    - <Button icon={<Array />} /> (<span style={{color: 'red'}}>12</span>) Details
    - <Button icon={<Layout />} /> (<span style={{color: 'red'}}>13</span>) Trajectories
    - <Button icon={<LayoutBalloon />} /> (<span style={{color: 'red'}}>14</span>) Relative trajectories
  </div>
  <div>![](/sse-14/visualisation/006.png)</div>
</div>
