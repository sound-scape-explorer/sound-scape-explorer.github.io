import {ExportingBlogBlock} from '@site/src/components/ExportingBlogBlock';
import {ExportingDocsBlock} from '@site/src/components/ExportingDocsBlock';
import {
  ChromeIcon,
  CseIcon,
  InstallIcon,
  PwaIcon,
} from '@site/src/components/Icons';
import {Fragment} from 'react';
import {VERSION} from '../../env';

# 🔌 Offline Access

## <PwaIcon /> Progressive Web App

<Fragment>
  <PwaIcon /> Progressive Web Applications can be installed using <ChromeIcon />{' '}
  Chrome browser.
</Fragment>

<Fragment>
  Click on the <InstallIcon /> icon in the address bar to install the
  application as standalone with offline caching.
</Fragment>

:::info

<Fragment>
  You can install <CseIcon /> CSE Docs and 👁️‍🗨️ [Visualisation
  Online](../visualisation-online/) as <PwaIcon /> PWAs.
</Fragment>

<Fragment>
  Because both applications are hosted under the same domain, you will need to
  install <b>first</b> the 👁️‍🗨️ [Visualisation Online](../visualisation-online/)
  module then <CseIcon /> CSE Docs.
</Fragment>

:::

## 📤 Export Documentation

:::warning
An active internet connection is necessary to retrieve data for exporting.
:::

:::note
Certain hypertext links may not be converted.
:::

Use the commands below to export the entire documentation to PDF.

To download the current {VERSION} version of the documentation, execute:

<ExportingDocsBlock
  version={VERSION}
  isCse
/>

## 📤 Export Blog

:::warning
An active internet connection is necessary to retrieve data for exporting.
:::

Execute the command below to export the blog content to PDF:

<ExportingBlogBlock />
