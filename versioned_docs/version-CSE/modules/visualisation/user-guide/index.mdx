---
sidebar_position: 2
---

import {ExcelIcon} from '@site/src/components/Icons';
import {
  DownloadPngIcon,
  DownloadSvgIcon,
  ExportCsvIcon,
  OrbitalIcon,
  PanIcon,
  ResetCameraIcon,
  TurntableIcon,
  ZoomIcon,
} from '@site/src/components/ScatterIcons';
import {
  AudioIcon,
  ColorsIcon,
  DetailsIcon,
  DigestedIcon,
  HelpIcon,
  ImportIcon,
  LabelsIcon,
  RelativeTrajectoriesIcon,
  SelectionIcon,
  SettingsIcon,
  TimeIcon,
  TrajectoriesIcon,
} from '@site/src/components/VisIcons';
import {Fragment} from 'react';

# 📖 User Guide

The visualisation interface relies on floating windows to organize the
available viewport efficiently.

In a minimalist design, each window can be effortlessly toggled between hidden
or shown states by interacting with the menu icons.

Discover more about the 🧭 [Menu](#-menu)
and 📊 [Plot controls](#-plot-controls) for additional help.

## 🧭 Menu

<Fragment>

| Icon                        | Function              | Description                                       | Keyboard Shortcut |
| --------------------------- | --------------------- | ------------------------------------------------- | ----------------- |
| <ImportIcon/>               | Import                | Load a local `.h5` storage file.                  | `i`               |
| <SettingsIcon/>             | Settings              | Adjust user interface settings.                   | `,`               |
| <HelpIcon/>                 | Help                  | Access help links and keyboard shortcuts.         | `h`               |
| <SelectionIcon/>            | Selection             | Select band, integration, extractor, and reducer. | `s`               |
| <ColorsIcon/>               | Colors                | Colour intervals by index, timestamp, or label.   | `c`               |
| <LabelsIcon/>               | Labels                | Filter intervals by labels.                       | `z`               |
| <TimeIcon/>                 | Time                  | Filter intervals by timestamps.                   | `t`               |
| <DetailsIcon/>              | Details               | Show details about the selected interval.         | `d`               |
| <AudioIcon/>                | Audio                 | Show audio for the selected interval.             | `a`               |
| <TrajectoriesIcon/>         | Trajectories          | Display trajectories.                             | `x`               |
| <RelativeTrajectoriesIcon/> | Relative Trajectories | Display relative trajectories.                    | `r`               |
| <DigestedIcon/>             | Digested              | Display digested metrics.                         | `b`               |

</Fragment>

## 📊 Plot controls

<Fragment>

| Icon               | Function     | Description                            |
| ------------------ | ------------ | -------------------------------------- |
| <ZoomIcon/>        | Zoom         | Set cursor to zoom mode.               |
| <PanIcon/>         | Pan          | Set cursor to pan mode.                |
| <OrbitalIcon/>     | Orbital      | Set cursor to orbital rotation mode.   |
| <TurntableIcon/>   | Turntable    | Set cursor to turntable rotation mode. |
| <ResetCameraIcon/> | Reset camera | Reset camera to default position.      |
| <DownloadPngIcon/> | Download PNG | Download scatter as PNG.               |
| <DownloadSvgIcon/> | Download SVG | Download scatter as SVG.               |
| <ExportCsvIcon/>   | Eport CSV    | Export scatter as CSV.                 |

</Fragment>

<Fragment>

## ⏳ Loading a new <ExcelIcon /> configuration

</Fragment>

Focus your terminal window and press `CTRL+C` keys to interrupt the module's services.

<Fragment>

:::tip

The 👁️‍🗨️ Visualisation module can handle only one <ExcelIcon />
configuration file at a time.

You will need to stop current services in order to load a [new one](../getting-started/).

:::

</Fragment>
